# No environment variables are required yet — no backend is connected.
# This file documents where future secrets should go once a backend
# (email API, Supabase, Firebase, custom API, etc.) is wired up in
# src/lib/submissionService.ts. Never commit a real .env file.
#
# Example (uncomment and fill in once a backend is chosen):
# VITE_API_URL=
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
